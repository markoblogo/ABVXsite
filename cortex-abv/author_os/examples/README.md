# AUTHOR_OS Examples

This directory keeps non-executable evidence examples for governance.

- `good/` — author-approved positive examples and rule demonstrations
- `bad/` — rejected examples with failure rationale
- `pending/` — draft examples awaiting owner review

The loader does not read these examples at runtime.
